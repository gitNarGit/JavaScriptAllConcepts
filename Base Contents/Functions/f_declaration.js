// this is a Function object
// they can't be created in conditions. For that purpose we can use function expressions
// function declarations are hoisted
sum(10,20); 
funciton sum(x,y){
  return x+y;
}
